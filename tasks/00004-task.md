[accepted]

# Жёсткое правило перечитывания task-файла и ограничения области работ

Важно: выполнение плана из этого файла запрещено, пока в начале файла не будет добавлена строка `[accepted]`.

## Контекст
- Я выполнил действия по устаревшему плану, не перечитав task-файл целиком после правок.
- Нужно исключить повторение: строго фиксируем правило перечитывания и соблюдения ограничений области работ.

## Цель
- Закрепить правило: перед любым действием перечитывать task-файл целиком и подтверждать ограничения.
- Запретить работы вне явных границ, указанных в task-файле.

## План работ
1) **Документация**
   - Обновить `docs/WORKFLOW.md`:
     - требование перечитывать task-файл перед началом работ;
     - требование явно перечислять ограничения/границы задачи в ответе перед выполнением;
     - запрет на изменения файлов, не перечисленных в task‑плане.
   - При необходимости добавить короткую ссылку/напоминание в `docs/agents/repo-map.md` и `docs/coding-rules.md`.
2) **Проверка**
   - Убедиться, что нет противоречий с текущими правилами.

